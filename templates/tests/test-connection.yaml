apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fleet-bootstrap.fullname" . }}-test-connection"
  labels:
    {{- include "fleet-bootstrap.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fleet-bootstrap.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
