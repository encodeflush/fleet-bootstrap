apiVersion: v1
kind: Service
metadata:
  name: {{ include "fleet-bootstrap.fullname" . }}
  labels:
    {{- include "fleet-bootstrap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fleet-bootstrap.selectorLabels" . | nindent 4 }}
